---
name: Bug report
about: Something is wrong or unsafe
labels: bug
---

**What happened**

**What you expected**

**Steps to reproduce**

**Environment**
- macOS version:
- Node version (`node --version`):
- Dockmaster commit (`git rev-parse --short HEAD`):
- Mode: `npm run dev` / `npm start` / LaunchAgent
- Local or remote machine:

**Logs**
Relevant lines from `~/.dockmaster/logs/` or the terminal, with secrets removed.
